import 'package:flutter/material.dart';

class FifthPages extends StatelessWidget {
  const FifthPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          "FifthPages",
        ),
      ),
    );
  }
}
