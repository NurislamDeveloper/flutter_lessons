import 'package:flutter/material.dart';

class NightPages extends StatelessWidget {
  const NightPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          "NightPages",
        ),
      ),
    );
  }
}
