import 'package:flutter/material.dart';

class SevemthPages extends StatelessWidget {
  const SevemthPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          "seventhPages",
        ),
      ),
    );
  }
}
