import 'package:flutter/material.dart';

class TenthPages extends StatelessWidget {
  const TenthPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          "TenthPages",
        ),
      ),
    );
  }
}
